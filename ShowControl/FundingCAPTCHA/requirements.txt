# FundingCAPTCHA dependencies
# Install with: pip install -r requirements.txt
#
# On Raspberry Pi, prefer system packages for numpy (faster ARM builds):
#   sudo apt-get install python3-numpy python3-scipy

# ── Always required ────────────────────────────────────────────────────────────
pygame>=2.1
websockets>=11
numpy>=1.24
python-osc>=1.8
Pillow>=9.0          # JPEG fallback for SDL_image builds without JPEG (Pi)

# ── CV pipeline (required only for --camera or --mock-camera) ─────────────────
scipy>=1.10
pyorbbecsdk2
