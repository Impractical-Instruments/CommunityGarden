# FundingCAPTCHA server dependencies
# Install with: pip install -r requirements.txt
#
# On Raspberry Pi, prefer system packages for numpy/scipy (faster ARM builds):
#   sudo apt-get install python3-numpy python3-scipy

# ── Core server (always required) ─────────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.23
websockets>=11
python-osc>=1.8

# ── CV pipeline (required only for --camera or --mock-camera) ─────────────────
numpy>=1.24
scipy>=1.10

# Orbbec camera SDK
pyorbbecsdk2

# ── Touch tester (touch_test_pygame.py) ───────────────────────────────────────
pygame>=2.1